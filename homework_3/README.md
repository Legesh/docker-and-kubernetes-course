# docker-and-kubernetes-course

Homework 3

- run containers (Will create backend, frontend and mysql db):

```make up```


- stop containers:

```make down```


- run sh cmd inside container:

```make backend-bash```

- make db dump from container to local folder:

```make dump-db```