# docker-and-kubernetes-course

Homework 1

- run 10 containers:

```make run```

All containers using one image and have separated ports (nginx-app-1 -> 8081, nginx-app-2 -> 8082, ...)

- stop and delete all of them:

```make clean```